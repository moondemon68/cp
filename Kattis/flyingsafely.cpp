#include <bits/stdc++.h>

using namespace std;

int main () {
	int tc;
	cin >> tc;
	for (int t=1;t<=tc;t++) {
		int n,m;
		cin >> n >> m;
		int a[10005],b[10005];
		for (int i=1;i<=m;i++) {
			cin >> a[i] >> b[i];
		}
		cout << n-1 << endl;
		
	}
}
