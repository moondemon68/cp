#include <bits/stdc++.h>
#define fi first
#define se second
#define pb push_back
#define mp make_pair
#define MOD 1000000007
#define pii pair<int,int>
#define LL long long

using namespace std;

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n,q;
    cin >> n >> q;
    string s;
    cin >> s;
    bool a[100005];
    for (int i=1;i<=n;i++) {
      a[i]=s[i-1]-'0';
    }
    for (int i=1;i<=q;i++) {
      int t,x,y;
      cin >> t >> x >> y;
      if (t==1) {
        
      } else {

      }
    }
    return 0;
}
